{
  "name": "sensor-reporter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "postinstall": "npm run build",
    "build": "typings install && tsc",
    "build-watch": "typings install && tsc -w",
    "start": "node build/index.js",
    "test": "npm run build && istanbul cover ./node_modules/mocha/bin/_mocha build/**/*.unit.js --report lcovonly -- -R spec && cat coverage/coverage.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.14.0",
    "moment": "^2.14.1",
    "nconf": "^0.8.4",
    "superagent": "^2.1.0",
    "uuid": "^2.0.2",
    "when": "^3.7.7",
    "htu21d-i2c": "^0.1.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "istanbul": "^0.4.4",
    "mocha": "^2.5.2",
    "sinon": "^1.17.4",
    "typescript": "^1.8.10",
    "typings": "^1.3.2"
  }
}
